# هيكل المشروع

هذا الملف يوضح هيكل مشروع تطبيق قارئ المانجا وكيفية تنظيم الكود والموارد.

## المجلدات الرئيسية

```
com.mangareader.app/
├── data/                 # طبقة البيانات
│   ├── db/               # قاعدة البيانات المحلية
│   ├── download/         # نظام التنزيل
│   ├── network/          # طلبات الشبكة
│   ├── preferences/      # تفضيلات التطبيق
│   └── repository/       # تنفيذ المستودعات
├── di/                   # حقن التبعية (Dependency Injection)
├── domain/               # طبقة المجال
│   ├── model/            # نماذج البيانات
│   ├── repository/       # واجهات المستودعات
│   └── usecase/          # حالات الاستخدام
├── source/               # مصادر المانجا
│   ├── ar/               # المصادر العربية
│   ├── en/               # المصادر الإنجليزية
│   ├── online/           # المصادر عبر الإنترنت
│   └── local/            # المصادر المحلية
├── ui/                   # واجهة المستخدم
│   ├── browse/           # شاشة التصفح
│   ├── details/          # شاشة تفاصيل المانجا
│   ├── downloads/        # شاشة التنزيلات
│   ├── library/          # شاشة المكتبة
│   ├── reader/           # شاشة القراءة
│   └── settings/         # شاشة الإعدادات
├── util/                 # أدوات مساعدة
│   ├── extension/        # ملحقات Kotlin
│   └── security/         # أدوات الأمان وتجاوز الحماية
├── App.kt                # فئة التطبيق الرئيسية
└── MainActivity.kt       # النشاط الرئيسي
```

## وصف المكونات الرئيسية

### طبقة البيانات (Data Layer)

تحتوي على جميع مصادر البيانات والتفاعلات مع قاعدة البيانات والشبكة:

- **db**: تعريفات قاعدة البيانات باستخدام Room
- **download**: نظام تنزيل الفصول والمانجا
- **network**: التعامل مع طلبات الشبكة باستخدام Retrofit و OkHttp
- **preferences**: إدارة تفضيلات المستخدم
- **repository**: تنفيذ واجهات المستودعات المعرفة في طبقة المجال

### طبقة المجال (Domain Layer)

تحتوي على منطق الأعمال الأساسي للتطبيق:

- **model**: نماذج البيانات الأساسية (Manga، Chapter، إلخ)
- **repository**: واجهات المستودعات التي تحدد العمليات المتاحة
- **usecase**: حالات الاستخدام التي تنفذ منطق الأعمال

### مصادر المانجا (Source Layer)

تحتوي على تنفيذات لمختلف مصادر المانجا:

- **ar**: المصادر العربية (مانجا سوات، مانجا ليونز، تيم إكس)
- **en**: المصادر الإنجليزية
- **online**: المصادر عبر الإنترنت
- **local**: المصادر المحلية (الملفات المخزنة على الجهاز)

### واجهة المستخدم (UI Layer)

تحتوي على جميع مكونات واجهة المستخدم:

- **browse**: شاشة تصفح المصادر والمانجا
- **details**: شاشة عرض تفاصيل المانجا وقائمة الفصول
- **downloads**: شاشة إدارة التنزيلات
- **library**: شاشة عرض المكتبة الشخصية
- **reader**: شاشة قراءة الفصول
- **settings**: شاشة الإعدادات والتفضيلات

### أدوات مساعدة (Util Layer)

تحتوي على أدوات وملحقات مساعدة:

- **extension**: ملحقات Kotlin لتبسيط العمليات المتكررة
- **security**: أدوات الأمان وتجاوز تقنيات الحماية مثل CloudFlare

## التقنيات المستخدمة

- **Kotlin**: لغة البرمجة الأساسية
- **Coroutines**: للبرمجة غير المتزامنة
- **Flow**: لتدفق البيانات التفاعلي
- **Hilt**: لحقن التبعية
- **Room**: لقاعدة البيانات المحلية
- **Retrofit & OkHttp**: للاتصال بالشبكة
- **Glide**: لتحميل وعرض الصور
- **Material Design**: لواجهة المستخدم
- **WorkManager**: لإدارة المهام في الخلفية
- **Jetpack Navigation**: للتنقل بين الشاشات
- **DataStore**: لتخزين التفضيلات
